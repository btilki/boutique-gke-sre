# ClusterSecretStore scaffold — enable in Phase 4 after ESO operator and Workload Identity are ready.
# References GCP Secret Manager via Workload Identity (no JSON key files).
#
# apiVersion: external-secrets.io/v1beta1
# kind: ClusterSecretStore
# metadata:
#   name: gcp-secret-manager
# spec:
#   provider:
#     gcpsm:
#       projectID: boutique-gke
#       auth:
#         workloadIdentity:
#           clusterLocation: europe-west1          # set to actual region
#           clusterName: boutique-gke              # set to actual cluster name
#           serviceAccountRef:
#             name: external-secrets
#             namespace: external-secrets
