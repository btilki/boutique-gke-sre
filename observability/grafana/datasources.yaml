# Grafana datasource provisioning — boutique-gke-sre
# Phase 6: mount as ConfigMap; Grafana sidecar or provisioning volume.
#
# Datasources:
#   - Google Managed Prometheus (default metrics)
#   - Cloud Monitoring (GCP-native SLOs and uptime checks)
#   - Cloud Trace (trace-to-metrics correlation, optional)

# apiVersion: 1
# datasources:
#   - name: Google Managed Prometheus
#     type: prometheus
#     access: proxy
#     isDefault: true
#     jsonData:
#       httpMethod: POST
#       prometheusType: Mimir
#       prometheusVersion: 2.40.0
#     secureJsonData: {}
#     # url: set via Grafana GCE/Workload Identity plugin or proxy
#
#   - name: Cloud Monitoring
#     type: stackdriver
#     access: proxy
#     jsonData:
#       authenticationType: gce
#       defaultProject: boutique-gke
#
#   - name: Cloud Trace
#     type: googlecloud-trace
#     access: proxy
#     jsonData:
#       authenticationType: gce
#       defaultProject: boutique-gke
