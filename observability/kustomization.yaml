# Observability stack — Kustomize root for Argo CD (Phase 6).
# Grafana and Prometheus are added in later steps of topic 13.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - otel/collector
